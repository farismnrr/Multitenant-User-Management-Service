// This is a placeholder file for local development and build processes.
// The actual content is served dynamically by the backend at runtime.
// Note: Only public values - NO secrets, API keys, or credentials.
window.config = {
    ENDPOINT: ""
};
